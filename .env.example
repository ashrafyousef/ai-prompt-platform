DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ai_prompt_platform?schema=public"
NEXTAUTH_SECRET="replace-with-a-long-random-string"
NEXTAUTH_URL="http://localhost:3000"
# Use Groq (free, fast) or OpenAI. GROQ_API_KEY takes priority if both are set.
GROQ_API_KEY=""
OPENAI_API_KEY=""
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""
TOKEN_SOFT_LIMIT="200000"
CHAT_CONTEXT_TOKEN_LIMIT="2500"

# Optional model governance (comma-separated registry ids). If unset, all runtime-available
# models follow registry + role rules. If set, only listed ids are enabled.
# CHAT_ENABLED_MODEL_IDS="openai-gpt-5.5,openai-gpt-5.4,openai-gpt-5.4-mini,..."
# Recommended when pinning defaults (set in Vercel only if you want overrides; not required):
# CHAT_DEFAULT_MODEL_ID="openai-gpt-5.4-mini"
# CHAT_FALLBACK_MODEL_ID="openai-gpt-4o-mini"

# Optional: email to receive ADMIN role when running `npm run prisma:seed`
ADMIN_SEED_EMAIL="demo@example.com"

# --- Vercel production ---
# Set these in Vercel → Project → Settings → Environment Variables (Production).
# NEXTAUTH_URL must match your deployment URL exactly, e.g.:
#   https://ai-prompt-platform-sepia.vercel.app
# NEXTAUTH_SECRET: run `openssl rand -base64 32` and paste the result.
# DATABASE_URL: your hosted Postgres (Neon, Supabase, Vercel Postgres, etc.).
# After changing env vars, redeploy.
